[phases.setup]
nixPkgs = ["python312", "nodejs_20"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt", "cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build", "ls -R frontend/dist"]

[phases.start]
cmd = "python3 backend/main.py"
